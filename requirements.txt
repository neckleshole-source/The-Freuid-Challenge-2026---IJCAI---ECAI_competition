# This is a comment inside a requirements file
requests==2.31.0      # Must be exactly this version
numpy>=1.24.0         # Must be this version or newer
pandas<=2.1.0         # Must be this version or older
scikit-learn~=1.3.0   # Compatible release (any 1.3.x version)
matplotlib            # Installs the latest available version
